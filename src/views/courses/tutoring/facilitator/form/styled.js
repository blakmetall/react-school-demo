import styled from 'styled-components';
import PerfectScrollbar from 'react-perfect-scrollbar';

const StyledAvatarsWrapper = styled(PerfectScrollbar)`
    max-height: 300px;
    overflow-y: scroll;
    background-color: #f2f7fb;
`;

export { StyledAvatarsWrapper };

import styled from 'styled-components';
import theme from '../../../../../theme';

export const StyledVideoAccess = styled.div`
    display: flex;
    border: 0.8px solid ${theme.bootstrap.appBlue2};
`;
